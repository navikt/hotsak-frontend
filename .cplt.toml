# Generated by `cplt init` — review before trusting.
# Detected: Node.js, Docker, Go
# Workspace members:
#   server (go.work) — Go

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: (root)
allow_docker = true

[propose.allow]
ports = [3000]

