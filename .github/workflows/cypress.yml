name: Cypress Tests

on:
  push:
    branches-ignore:
      - master # deploy-dev.yml har egen cypress test

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          start: npm run dev:mock
          wait-on: 'http://localhost:3001'
