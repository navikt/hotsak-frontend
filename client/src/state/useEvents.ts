/*
import { useState } from 'react'

export function useEvents() {
  const [state, setState] = useState<unknown>(null)
  const source = new EventSource('/api/sak/1000')
  source.addEventListener('onmessage', (event) => console.log(event.data))
  return state
}
*/
