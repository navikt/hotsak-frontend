{
  "name": "hotsak-frontend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "cross-env": "^10.0.0",
    "eslint": "^9.35.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "build:client": "npm --prefix client run build",
    "build:server": "go build -C server ./...",
    "build": "npm-run-all build:*",
    "test:client": "npm --prefix client run test:ci",
    "test:server": "go test -C server ./...",
    "test": "npm-run-all test:*",
    "dev": "npm --prefix client run dev",
    "dev:server": "<read .env> && go run -C ./server .",
    "cypress:open": "npm --prefix client run cypress:open",
    "cypress:run": "npm --prefix client run cypress:run",
    "prepare": "husky || true",
    "lint:client": "npm --prefix client run lint",
    "lint:server": "go vet -C server ./...",
    "lint": "npm-run-all lint:*"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,json,md}": [
      "prettier --write"
    ],
    "*.{ts,tsx}": [
      "eslint --fix"
    ]
  }
}
